@model Core.Models.Categoria

@{
    ViewData["Title"] = "ViewCategoria";
}

<h1>ViewCategoria</h1>

<div>
    <h4>Categoria</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Id)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Id)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Descricao)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Descricao)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-sm btn-danger btn-icon" asp-controller="categoria" asp-action="EditCategoria" asp-route-id="@Model.Id"><i class="far fa-edit"></i></a>
    <a class="btn btn-sm btn-danger btn-icon" asp-controller="categoria" asp-action="Index"><i class="fas fa-undo"></i></a>
</div>
